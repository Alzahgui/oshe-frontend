services:
  oshe-frontend:
    image: ${IMAGE}
    container_name: oshe-frontend
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL}
